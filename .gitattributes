# Кит и артборды хранятся побайтно, как на платформе: проверка импорта сверяет размеры
# файлов, и клон на Windows с core.autocrlf=true не должен переписывать переводы строк.
kit/** -text
bench/artboards/** -text

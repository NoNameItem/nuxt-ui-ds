<template>
  <!-- Ширина обёртки набирается распоркой из готовых утилит, а не произвольным значением:
       `w-[1600px]` Tailwind сгенерировал бы на нашей стороне и не сгенерировал на китовой —
       CSS кита это дамп утилит, собранный по его собственным исходникам, и класса, которого
       кит не видел, там нет. Обёртка вышла бы шириной 1280 только на ките, и мы получили бы
       поддельное расхождение вместо наблюдаемого свойства (проверено по
       dist/docs/kitstatic/tokens, nuxt-ui-ds-w9w).

       Зачем ширина вообще: токен `--ui-container` равен 80rem = 1280px, кадр съёмки тоже
       1280 — при равной ширине `max-w-(--ui-container)` ничего не ограничивает, а `mx-auto`
       центрует при нулевом остатке, и оба класса темы нулём дифа не проверяются. Пять
       распорок по 24rem дают 1920: контейнер зажимается до 1280 и центруется с остатком
       320px, а заливка внутренней коробки делает края наблюдаемыми. -->
  <div class="w-max">
    <div class="flex">
      <div v-for="i in 5" :key="i" class="w-96 h-1 shrink-0" />
    </div>
    <UContainer>
      <div class="bg-elevated p-4">
        <p>Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore.</p>
      </div>
    </UContainer>
  </div>
</template>

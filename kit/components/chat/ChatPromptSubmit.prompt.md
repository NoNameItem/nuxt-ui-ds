ChatPromptSubmit — Nuxt UI 4.11.1 `UChatPromptSubmit`; classes come straight from themes/chat-prompt-submit.ts.

```jsx
<ChatPromptSubmit>Content</ChatPromptSubmit>
```

Slots (each is a prop taking ReactNode): base

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
ChatShimmer — Nuxt UI 4.11.1 `UChatShimmer`; classes come straight from themes/chat-shimmer.ts.

```jsx
<ChatShimmer>Content</ChatShimmer>
```

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
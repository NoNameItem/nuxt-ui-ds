Container — Nuxt UI 4.11.1 `UContainer`; classes come straight from themes/container.ts.

```jsx
<Container>Content</Container>
```

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
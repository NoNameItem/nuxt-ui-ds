Link — Nuxt UI 4.11.1 `ULink`; classes come straight from themes/link.ts.

```jsx
<Link active="true" disabled="true">Content</Link>
```

Variants: `active` (true | false), `disabled` (true)

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
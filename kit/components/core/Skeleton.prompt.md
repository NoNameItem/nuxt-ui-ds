Skeleton — Nuxt UI 4.11.1 `USkeleton`; classes come straight from themes/skeleton.ts.

```jsx
<Skeleton>Content</Skeleton>
```

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
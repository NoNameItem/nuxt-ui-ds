Tooltip — Nuxt UI 4.11.1 `UTooltip`; classes come straight from themes/tooltip.ts.

```jsx
<Tooltip>Content</Tooltip>
```

Slots (each is a prop taking ReactNode): content, arrow, text, kbds, kbdsSize

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
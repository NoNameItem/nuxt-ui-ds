DashboardGroup — Nuxt UI 4.11.1 `UDashboardGroup`; classes come straight from themes/dashboard-group.ts.

```jsx
<DashboardGroup>Content</DashboardGroup>
```

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
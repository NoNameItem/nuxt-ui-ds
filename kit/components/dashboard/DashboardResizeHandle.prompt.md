DashboardResizeHandle — Nuxt UI 4.11.1 `UDashboardResizeHandle`; classes come straight from themes/dashboard-resize-handle.ts.

```jsx
<DashboardResizeHandle>Content</DashboardResizeHandle>
```

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
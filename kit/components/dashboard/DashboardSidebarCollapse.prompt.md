DashboardSidebarCollapse — Nuxt UI 4.11.1 `UDashboardSidebarCollapse`; classes come straight from themes/dashboard-sidebar-collapse.ts.

```jsx
<DashboardSidebarCollapse side="left">Content</DashboardSidebarCollapse>
```

Variants: `side` (left | right)

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
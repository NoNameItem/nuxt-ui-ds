DashboardSidebarToggle — Nuxt UI 4.11.1 `UDashboardSidebarToggle`; classes come straight from themes/dashboard-sidebar-toggle.ts.

```jsx
<DashboardSidebarToggle side="left">Content</DashboardSidebarToggle>
```

Variants: `side` (left | right)

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
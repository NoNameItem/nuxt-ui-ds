Form — Nuxt UI 4.11.1 `UForm`; classes come straight from themes/form.ts.

```jsx
<Form>Content</Form>
```

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
Footer — Nuxt UI 4.11.1 `UFooter`; classes come straight from themes/footer.ts.

```jsx
<Footer>Content</Footer>
```

Slots (each is a prop taking ReactNode): root, top, bottom, container, left, center, right

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
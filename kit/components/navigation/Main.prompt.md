Main — Nuxt UI 4.11.1 `UMain`; classes come straight from themes/main.ts.

```jsx
<Main>Content</Main>
```

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
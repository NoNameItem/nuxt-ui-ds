BlogPosts — Nuxt UI 4.11.1 `UBlogPosts`; classes come straight from themes/blog-posts.ts.

```jsx
<BlogPosts orientation="horizontal">Content</BlogPosts>
```

Variants: `orientation` (horizontal | vertical)

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
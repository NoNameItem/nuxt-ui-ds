PageBody — Nuxt UI 4.11.1 `UPageBody`; classes come straight from themes/page-body.ts.

```jsx
<PageBody>Content</PageBody>
```

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
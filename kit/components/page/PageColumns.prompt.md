PageColumns — Nuxt UI 4.11.1 `UPageColumns`; classes come straight from themes/page-columns.ts.

```jsx
<PageColumns>Content</PageColumns>
```

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
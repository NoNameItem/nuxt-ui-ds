PageGrid — Nuxt UI 4.11.1 `UPageGrid`; classes come straight from themes/page-grid.ts.

```jsx
<PageGrid>Content</PageGrid>
```

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
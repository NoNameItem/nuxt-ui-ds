PageList — Nuxt UI 4.11.1 `UPageList`; classes come straight from themes/page-list.ts.

```jsx
<PageList divide="true">Content</PageList>
```

Variants: `divide` (true)

Override any slot with `ui={{ slot: "classes" }}`. Overlays take `open` as a prop — no behaviour.
# Chat kit

Recreation of the Nuxt UI Pro chat template.

- `index.html` — type in the composer and press Enter; a mocked reply arrives after a beat.
- `ChatApp.jsx` — thread rail, message list with hover actions and code blocks, composer built on `chat-prompt.ts` proportions.

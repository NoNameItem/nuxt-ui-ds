# Docs kit

Recreation of the Nuxt UI documentation site.

- `index.html` — click a component in the aside nav to change the page.
- `DocsSite.jsx` — header (`header.ts`), aside navigation (`navigation-menu.ts` link classes), prose body with a live component preview block, on-this-page rail (`page-anchors.ts`), prev/next links.
